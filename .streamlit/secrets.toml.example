# Copie ce fichier en .streamlit/secrets.toml (NON commité) pour tester l'auth en local,
# OU renseigne cette clé dans l'interface Streamlit Community Cloud (Settings > Secrets).
# Si la clé est absente, le dashboard est en accès libre (pratique en local).
app_password = "choisis-un-mot-de-passe"
